pandas: ~2.2.0 (for data manipulation and analysis)
numpy: ~1.26.0 (for numerical operations)
seaborn: ~0.13.0 (for data visualization)
matplotlib: ~3.8.0 (for plotting and visualization)
scikit-learn: ~1.4.0 (for machine learning models, preprocessing, and evaluation metrics, including LabelEncoder, StandardScaler, train_test_split, DecisionTreeClassifier, RandomForestClassifier, LogisticRegression, GradientBoostingClassifier, SVC, GridSearchCV, RandomizedSearchCV, classification_report, accuracy_score, confusion_matrix, mean_absolute_error, r2_score, cross_val_score)
joblib: ~1.3.0 (for model persistence)
gradio: ~6.0.0 (for deploying interface)
